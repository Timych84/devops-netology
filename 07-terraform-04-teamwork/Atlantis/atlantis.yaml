version: 3
projects:
- dir: .
  workspace: stage
  autoplan:
    when_modified: ["*.tf*"]
    enabled: true
  workflow: stage
- dir: .
  workspace: prod
  autoplan:
    when_modified: ["*.tf*"]
    enabled: true
  workflow: prod
